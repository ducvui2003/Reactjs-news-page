import { CustomPalette } from '../type';

const lightPalette: CustomPalette = {
  mode: 'light',
  primary: {
    main: '#1976d2',
  },
  secondary: {
    main: '#dc004e',
  },
};
export default lightPalette;
